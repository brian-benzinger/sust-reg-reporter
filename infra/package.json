{
  "name": "@sust-reg/infra",
  "version": "0.0.0",
  "private": true,
  "description": "AWS CDK infrastructure (ADR-0015): the $1 cost backstop, the snapshotting pipeline, and the serving layer — all inside AWS Always Free (ADR-0016).",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy",
    "destroy": "cdk destroy"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.180.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "aws-cdk": "^2.1018.0"
  }
}
