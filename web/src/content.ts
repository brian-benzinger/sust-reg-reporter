/**
 * Standing site copy that must not drift between pages.
 *
 * The non-interpretive disclaimer (ADR-0002) is centralized here so every page
 * — static or hydrated — renders the identical text.
 */
export const NOT_LEGAL_ADVICE =
  "Not legal advice. This site returns primary-source text, citations, " +
  "effective dates, and applicability metadata only. Verify against the cited " +
  "source and consult qualified counsel.";

export const SITE_NAME = "sust-reg-reporter";
