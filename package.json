{
  "name": "sust-reg-reporter",
  "version": "0.0.0",
  "private": true,
  "description": "Version-tracked climate disclosure regulations with point-in-time history, sourced citations, and per-company applicability.",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "core",
    "infra",
    "web"
  ],
  "scripts": {
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "typecheck": "tsc -p core/tsconfig.json && tsc -p infra/tsconfig.json && tsc -p web/tsconfig.json",
    "synth": "npm run synth -w @sust-reg/infra",
    "build:web": "npm run build -w @sust-reg/web"
  },
  "engines": {
    "node": ">=22.18"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "@vitest/coverage-v8": "^4.1.7",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
